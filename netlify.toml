[build]
command = "npm ci && npm run build"
functions = "netlify/functions"
publish = "dist"

[build.environment]
NODE_VERSION = "22"
NODE_ENV = "production"
CI = "true"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
